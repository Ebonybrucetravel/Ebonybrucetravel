'use client';

// The auth modal popup is rendered by AppShell when the pathname is /login.
// This page exists so the route resolves (no 404). It renders nothing —
// the overlay in AppShell handles the UI.
export default function LoginPage() {
  return null;
}
